# Rust build
/target

# Large / regenerable model artifacts (kept locally, not committed)
/assets/
*.engine
*.calib
*.onnx
*.onnx.data
*.safetensors
*.pkl

# Media (regenerable); keep the small test fixtures
*.mp4
*.mkv
!demo/test_720p.mp4
!demo/test_10s.mp4

# Caches / envs
**/__pycache__/
**/*_cache/
.venv/
.playwright-mcp/

# Local / machine-specific config & secrets — DO NOT COMMIT
opencode.json
/.claude/
/.cargo/

# Vendored TensorRT SDK (NVIDIA headers + import lib generated from the DLL).
# Not redistributed; regenerate locally — see AGENTS.md "Bootstrap third_party".
/third_party/

# Scratch
/frames_in/
/frames_out/
